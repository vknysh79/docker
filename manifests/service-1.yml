apiVersion: v1
kind: Service
metadata:
    name: "dockertest-865d"
    labels:
        app: "dockertest-865d"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "dockertest-865d"